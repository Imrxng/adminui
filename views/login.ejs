<!DOCTYPE html>
<html lang="nl-BE">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>One Piece | onepiece</title>
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="shortcut icon" href="/assets/icons/pirateskull.svg" type="image/x-icon">
    <script src="https://kit.fontawesome.com/d59bc2ea1d.js" crossorigin="anonymous"></script>
</head>
<body id="bodyLogin">
    <main id="mainLogin">
        <button id="submit" style="padding: 10px; margin: 0rem 0 2rem 20rem;">
            <% if (word === "register") { %>
            <a href="/login" style="color: white;">login</a>
            <% } else { %>
            <a href="/registerReset" style="color: white;">register</a>
            <% } %>
        </button>
        <h1> <%= word.toUpperCase() %> </h1>
        <form action="/<%= word %>" method="post" id="formLogin">
            <input type="text" name="username" id="username" placeholder="username">
            <input type="password" name="password" id="password" placeholder="password">
            <button id="submit"><%= word.toUpperCase() %></button>
        </form>
        <% if(notification) { %>
            <p style="color: #E23E40;"><%=notification%></p>
        <% } %>
    </main>
</body>
</html>